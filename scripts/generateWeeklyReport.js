const fs = require('fs'); const path = require('path'); const errors = JSON.parse(fs.readFileSync(path.join(__dirname, '../master_errors.json'), 'utf8')); const oneWeekAgo = new Date(); oneWeekAgo.setDate(oneWeekAgo.getDate() - 7); const newErrors = errors.filter(e => new Date(e.updatedAt) > oneWeekAgo); const report = `Weekly Report\nTotal: ${errors.length}\nNew: ${newErrors.length}`; fs.writeFileSync('weekly_report.txt', report); console.log('Report generated.');